# Goal Plan — add-cli (F6)

## 1. Contexto
- Repo: `C:/Users/Script7/Desktop/second-brain-mcp/`
- Stack: TS + commander
- Pré-req: F2 (storage), F4 (curator), F5 (skills) done

## 2. Estado final mensurável
- `src/cli/index.ts` bin entrypoint (#!/usr/bin/env node)
- Subcomandos via commander:
  - `brain init` — cria `~/.brain/`, db, config.toml default
  - `brain status` — mostra DB path, fact count, skill count, último curator run
  - `brain curator run` — força curator agora (sync)
  - `brain skills list [--scope]` — lista skills
  - `brain export --scope=<x>` — backup JSON stdout
- `package.json` campo `bin`: `{ "brain": "./dist/cli/index.js" }`
- `npm run build` gera dist/cli/index.js executável
- `npm link` ou `npx .` permite chamar `brain --version`
- Snapshot tests stdout de cada comando

## 3. Prova surfaceável
- Comandos:
  - `npm test -- cli 2>&1 | grep -E "Tests:|passed"`
  - `npm test -- --coverage 2>&1 | grep -E "src/cli"`
  - `npm run build 2>&1 | tail -5`
  - `node dist/cli/index.js --version 2>&1`
  - `node dist/cli/index.js init --help 2>&1 | head -10`
- Output esperado:
  - `Tests: N passed` com N≥12
  - coverage src/cli ≥80%
  - build sem erros
  - `--version` retorna `0.1.0`
  - `--help` lista subcomandos

## 4. Restrições

### Projeto
- NÃO usar `process.exit` em handlers — retornar exit code via return
- NÃO importar storage diretamente em commands — usar service layer
- Cada comando ≤50 linhas
- Sem `Manager`/`Helper`/`Util`
- Sem boolean flag param — usar option strings (`--scope`)
- Errors estruturados com mensagem clara ao user

### Padrão
- TDD obrigatório
- NÃO --no-verify, NÃO @ts-ignore, NÃO lint disable
- NÃO modificar lockfiles
- Commits descritivos

## 5. Bound
- 15 turns

## 7. Condição final

```
Implementar src/cli/ do second-brain-mcp via commander. Estado final: bin entrypoint src/cli/index.ts com shebang, subcomandos brain init (cria ~/.brain + db + config.toml), brain status (DB path/fact count/skill count/ultimo curator run), brain curator run (forca curator sync), brain skills list (--scope), brain export --scope=<x> (backup JSON stdout), package.json bin field aponta para dist/cli/index.js. Provar com `npm test -- cli 2>&1 | grep "Tests:"` mostrando >=12 testes passed 0 failed incluindo snapshot stdout de cada comando, `npm test -- --coverage 2>&1 | grep "src/cli"` >=80% coverage, `npm run build 2>&1 | tail -5` sem erros, `node dist/cli/index.js --version` retorna 0.1.0, `node dist/cli/index.js init --help | head -10` lista flags. Sem process.exit em handlers (retornar exit via return), sem importar storage direto em commands (service layer), cada comando <=50 linhas, sem Manager/Helper/Util, sem boolean flag param (option strings), errors estruturados com mensagem clara, TDD obrigatorio, sem --no-verify, sem @ts-ignore, sem desabilitar lint, sem modificar lockfiles, sem mensagens de commit vagas, or stop after 15 turns. Report turn count, test count, coverage cli, build status e remaining bound each turn. Claude must echo full output of each verification command.
```

## 9. Checklist pré-entrega
- [x] ≤4000 chars
- [x] comandos + outputs
- [x] restrições
- [x] bound 15 turns
- [x] echo obrigatório
